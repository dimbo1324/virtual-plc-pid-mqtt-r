# Web UI

The local embedded dashboard will be implemented in a later stage.

The MVP will use HTML, CSS, and vanilla JavaScript without a separate frontend
build system. Go will serve the assets from the single application binary.
